import { Currency, Project, Expense } from '@prisma/client';
export function calculateSummary(project:Project,expenses:Expense[]){const cop=expenses.filter(e=>e.currency===Currency.COP).reduce((a,e)=>a+Number(e.totalValue),0),usd=expenses.filter(e=>e.currency===Currency.USD).reduce((a,e)=>a+Number(e.totalValue),0), rate=Number(project.exchangeRate||0); const total=project.baseCurrency===Currency.COP?cop+usd*rate:usd+(rate?cop/rate:0); const advance=Number(project.advanceAmount)*(project.advanceCurrency===project.baseCurrency?1:project.advanceCurrency===Currency.USD?rate:1/rate); return {totalCOP:cop,totalUSD:usd,totalBase:total,advanceBase:advance,balance:advance-total, message:advance>total?'USTED DEBE DEVOLVERLE EL SALDO A USCOM SAS':advance<total?'USTED TIENE UN SALDO A FAVOR':'LEGALIZACIÓN SIN SALDO PENDIENTE'} }
